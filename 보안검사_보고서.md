# 🔐 보안 검사 및 고도화 보고서
## 일자리도약장려금 관리 시스템

**검사 일자**: 2026-01-29  
**대상 사이트**: https://subsidy-management-qtjo.vercel.app/  
**검사 유형**: 정적 코드 분석 (Static Code Analysis)

---

## 📋 Executive Summary

전반적으로 **중간 수준의 보안 상태**를 보이고 있으며, 개인정보를 다루는 시스템임에도 불구하고 **여러 보안 취약점이 발견**되었습니다. 즉시 조치가 필요한 **Critical 레벨 3건**, 조기 대응이 필요한 **High 레벨 5건**을 포함하여 총 15개의 취약점을 발견했습니다.

### 심각도 요약
- 🔴 **Critical (치명적)**: 3건
- 🟠 **High (높음)**: 5건
- 🟡 **Medium (중간)**: 5건
- 🟢 **Low (낮음)**: 2건

---

## 🔴 Critical (치명적) - 즉시 조치 필요

### 1. 민감 정보 하드코딩 및 노출
**위치**: `server.js:13-19`
```javascript
const SUPABASE_URL = 'https://knkffxwcsrkxjneffyzh.supabase.co';
const SUPABASE_ANON_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...';
const JWT_SECRET = process.env.JWT_SECRET || 'subsidy-mgmt-jwt-secret-key-2026-very-secure-string';
```

**문제점**:
- Supabase 접속 정보가 소스코드에 직접 노출
- JWT 시크릿 키가 하드코딩되어 있음
- 공개 저장소에 푸시될 경우 DB 전체 노출

**영향**:
- 데이터베이스 전체 접근 가능
- JWT 토큰 위조 가능
- 개인정보 대량 유출 위험

**해결방안**:
```javascript
// ❌ 현재
const SUPABASE_URL = 'https://...';
const SUPABASE_ANON_KEY = 'eyJhbGci...';

// ✅ 권장
const SUPABASE_URL = process.env.SUPABASE_URL;
const SUPABASE_ANON_KEY = process.env.SUPABASE_ANON_KEY;

// .env 파일 (Git에서 제외)
SUPABASE_URL=https://knkffxwcsrkxjneffyzh.supabase.co
SUPABASE_ANON_KEY=eyJhbGci...
JWT_SECRET=임의의_강력한_비밀키_64자_이상
```

**긴급 조치**:
1. `.env` 파일로 즉시 이동
2. `.gitignore`에 `.env` 추가 확인
3. GitHub/Git 히스토리에서 민감정보 제거 (git filter-branch 또는 BFG Repo-Cleaner)
4. Supabase ANON_KEY 재발급
5. JWT_SECRET 변경 (기존 사용자 재로그인 필요)

---

### 2. CORS 정책 취약 - 모든 출처 허용
**위치**: `server.js:22-25`
```javascript
app.use(cors({
    origin: true,  // ⚠️ 모든 출처 허용
    credentials: true
}));
```

**문제점**:
- 모든 도메인에서 API 접근 허용
- CSRF 공격에 취약
- credentials: true와 함께 사용 시 매우 위험

**영향**:
- 악의적인 웹사이트에서 사용자의 인증 정보를 이용한 API 호출 가능
- 세션 하이재킹 위험

**해결방안**:
```javascript
// ✅ 권장
app.use(cors({
    origin: [
        'https://subsidy-management-qtjo.vercel.app',
        'http://localhost:3001'  // 개발 환경
    ],
    credentials: true,
    methods: ['GET', 'POST', 'PUT', 'DELETE'],
    allowedHeaders: ['Content-Type', 'Authorization']
}));
```

---

### 3. 비밀번호 해시 응답으로 반환
**위치**: `server.js:274-278`
```javascript
res.json({
    success: true,
    message: '비밀번호가 변경되었습니다...',
    newHash: newHashedPassword  // ⚠️ 해시를 클라이언트에 전송
});
```

**문제점**:
- 비밀번호 해시값을 HTTP 응답으로 전송
- 네트워크 스니핑 시 해시 탈취 가능
- Rainbow table 공격에 사용될 수 있음

**해결방안**:
```javascript
// ✅ 권장
// 1. 해시를 응답에 포함하지 않음
res.json({
    success: true,
    message: '비밀번호가 변경되었습니다. 콘솔을 확인하세요.'
});

// 2. 서버 측에서만 로그 출력
console.log('\n🔐 새 비밀번호 해시 (서버에서만 확인):');
console.log(newHashedPassword);

// 3. 더 나은 방법: 환경변수 자동 업데이트 스크립트 제공
const fs = require('fs');
const envPath = '.env';
const envContent = fs.readFileSync(envPath, 'utf8');
const updatedContent = envContent.replace(
    /ADMIN_PASSWORD_HASH=.*/,
    `ADMIN_PASSWORD_HASH=${newHashedPassword}`
);
fs.writeFileSync(envPath, updatedContent);
console.log('✅ .env 파일이 자동으로 업데이트되었습니다.');
```

---

## 🟠 High (높음) - 조기 대응 필요

### 4. 레이트 리미팅(Rate Limiting) 부재
**위치**: 로그인 API (`server.js:100`)

**문제점**:
- 로그인 시도 횟수 제한 없음
- Brute Force 공격에 취약
- DDoS 공격 가능

**해결방안**:
```javascript
// npm install express-rate-limit
const rateLimit = require('express-rate-limit');

// 로그인 제한
const loginLimiter = rateLimit({
    windowMs: 15 * 60 * 1000, // 15분
    max: 5, // 최대 5회 시도
    message: { 
        success: false, 
        error: '로그인 시도 횟수를 초과했습니다. 15분 후 다시 시도하세요.' 
    },
    standardHeaders: true,
    legacyHeaders: false
});

app.post('/api/auth/login', loginLimiter, async (req, res) => {
    // ... 로그인 로직
});

// 일반 API 제한
const apiLimiter = rateLimit({
    windowMs: 1 * 60 * 1000, // 1분
    max: 100 // 최대 100회
});

app.use('/api/', apiLimiter);
```

---

### 5. 쿠키 보안 설정 약함
**위치**: `server.js:145-150`
```javascript
res.cookie('auth_token', token, {
    httpOnly: true,
    secure: process.env.NODE_ENV === 'production',  // ⚠️ 개발 시 false
    sameSite: 'lax',  // ⚠️ CSRF 보호 약함
    maxAge: 24 * 60 * 60 * 1000
});
```

**문제점**:
- `sameSite: 'lax'`는 CSRF 보호 불충분
- 개발 환경에서 secure 플래그 비활성화

**해결방안**:
```javascript
// ✅ 권장
res.cookie('auth_token', token, {
    httpOnly: true,
    secure: true,  // HTTPS 필수
    sameSite: 'strict',  // CSRF 완전 차단
    maxAge: 24 * 60 * 60 * 1000,
    path: '/',
    domain: process.env.COOKIE_DOMAIN || undefined
});

// 개발 환경에서는 localhost에서 HTTPS 사용
// mkcert 등을 사용하여 로컬 SSL 인증서 생성 권장
```

---

### 6. 보안 헤더 미적용
**위치**: `server.js` 전체

**문제점**:
- Helmet 미들웨어 미사용
- X-Frame-Options, CSP 등 보안 헤더 없음
- Clickjacking, XSS 공격에 취약

**해결방안**:
```javascript
// npm install helmet
const helmet = require('helmet');

app.use(helmet({
    contentSecurityPolicy: {
        directives: {
            defaultSrc: ["'self'"],
            styleSrc: ["'self'", "'unsafe-inline'"],
            scriptSrc: ["'self'", "https://cdn.sheetjs.com"],
            imgSrc: ["'self'", "data:", "https:"],
            connectSrc: ["'self'"],
            fontSrc: ["'self'"],
            objectSrc: ["'none'"],
            mediaSrc: ["'self'"],
            frameSrc: ["'none'"]
        }
    },
    hsts: {
        maxAge: 31536000,
        includeSubDomains: true,
        preload: true
    },
    noSniff: true,
    xssFilter: true,
    referrerPolicy: { policy: 'strict-origin-when-cross-origin' }
}));
```

---

### 7. XSS(Cross-Site Scripting) 취약점 가능성
**위치**: 프론트엔드 전반 (특히 `app.js`에서 `innerHTML` 사용)

**문제점**:
- 사용자 입력값을 직접 DOM에 삽입
- 입력 필터링 및 이스케이핑 부족

**예시**:
```javascript
// app.js:316-327
companyNavList.innerHTML = `
    ...
    <a href="#" ... data-name="${c.name}">${c.name}</a>
    ...
`;
```

**해결방안**:
```javascript
// 1. DOMPurify 사용 (npm install dompurify)
import DOMPurify from 'dompurify';

const sanitizedName = DOMPurify.sanitize(c.name);
companyNavList.innerHTML = `
    <a href="#" ... data-name="${sanitizedName}">${sanitizedName}</a>
`;

// 2. 또는 textContent 사용 (HTML 파싱 안 함)
const nameSpan = document.createElement('span');
nameSpan.textContent = c.name;

// 3. 서버 사이드 검증
app.post('/api/companies', requireAuth, async (req, res) => {
    const { name } = req.body;
    
    // HTML 태그 제거
    const sanitizedName = name.replace(/<[^>]*>/g, '');
    
    // 또는 더 엄격하게: 영문/한글/숫자만 허용
    if (!/^[a-zA-Z0-9가-힣\s\-_()]+$/.test(name)) {
        return res.status(400).json({
            success: false,
            error: '기업명에 허용되지 않은 문자가 포함되어 있습니다.'
        });
    }
    
    // ... DB 저장
});
```

---

### 8. SQL Injection (간접) - 입력 검증 부족
**위치**: 여러 API 엔드포인트

**문제점**:
- Supabase Client 사용으로 직접적인 SQL Injection은 방어됨
- 그러나 입력값 검증이 부족하여 논리적 취약점 존재 가능

**해결방안**:
```javascript
// ✅ 입력 검증 미들웨어 추가
const { body, param, validationResult } = require('express-validator');

app.post('/api/companies', requireAuth, [
    body('name')
        .trim()
        .isLength({ min: 1, max: 100 })
        .withMessage('기업명은 1-100자여야 합니다')
        .matches(/^[a-zA-Z0-9가-힣\s\-_()]+$/)
        .withMessage('기업명에 허용되지 않은 문자가 포함되어 있습니다'),
    body('businessNumber')
        .optional()
        .matches(/^\d{3}-\d{2}-\d{5}$/)
        .withMessage('사업자번호 형식이 올바르지 않습니다'),
    body('email')
        .optional()
        .isEmail()
        .withMessage('올바른 이메일 형식이 아닙니다'),
    body('commission')
        .optional()
        .isFloat({ min: 0, max: 100 })
        .withMessage('수수료는 0-100 사이여야 합니다')
], async (req, res) => {
    const errors = validationResult(req);
    if (!errors.isEmpty()) {
        return res.status(400).json({ 
            success: false, 
            errors: errors.array() 
        });
    }
    
    // ... 로직
});
```

---

## 🟡 Medium (중간) - 단기 개선 권장

### 9. 세션 관리 취약
**문제점**:
- JWT 만료 시간이 24시간으로 너무 길다
- Refresh Token이 없어 만료 시 재로그인 필요
- 로그아웃 시 서버 측 토큰 무효화 불가

**해결방안**:
```javascript
// Access Token (짧은 수명) + Refresh Token (긴 수명) 구조
const accessToken = jwt.sign(
    { username, isAdmin: true },
    JWT_SECRET,
    { expiresIn: '15m' }  // 15분
);

const refreshToken = jwt.sign(
    { username, isAdmin: true },
    REFRESH_TOKEN_SECRET,
    { expiresIn: '7d' }  // 7일
);

// Redis 또는 DB에 Refresh Token 저장
await redis.set(`refresh:${username}`, refreshToken, 'EX', 7 * 24 * 60 * 60);

// 쿠키 설정
res.cookie('access_token', accessToken, {
    httpOnly: true,
    secure: true,
    sameSite: 'strict',
    maxAge: 15 * 60 * 1000  // 15분
});

res.cookie('refresh_token', refreshToken, {
    httpOnly: true,
    secure: true,
    sameSite: 'strict',
    path: '/api/auth/refresh',  // Refresh 전용 경로
    maxAge: 7 * 24 * 60 * 60 * 1000  // 7일
});

// Refresh Token 엔드포인트
app.post('/api/auth/refresh', async (req, res) => {
    const refreshToken = req.cookies.refresh_token;
    
    if (!refreshToken) {
        return res.status(401).json({ success: false, error: '토큰 없음' });
    }
    
    try {
        const decoded = jwt.verify(refreshToken, REFRESH_TOKEN_SECRET);
        
        // Redis에서 검증
        const storedToken = await redis.get(`refresh:${decoded.username}`);
        if (storedToken !== refreshToken) {
            throw new Error('유효하지 않은 토큰');
        }
        
        // 새 Access Token 발급
        const newAccessToken = jwt.sign(
            { username: decoded.username, isAdmin: decoded.isAdmin },
            JWT_SECRET,
            { expiresIn: '15m' }
        );
        
        res.cookie('access_token', newAccessToken, {
            httpOnly: true,
            secure: true,
            sameSite: 'strict',
            maxAge: 15 * 60 * 1000
        });
        
        res.json({ success: true });
    } catch (error) {
        res.status(401).json({ success: false, error: '토큰 갱신 실패' });
    }
});
```

---

### 10. 로깅 및 감사(Audit) 부족
**문제점**:
- 로그인 실패 기록 없음
- API 접근 로그 미흡
- 보안 이벤트 모니터링 부재

**해결방안**:
```javascript
// npm install winston
const winston = require('winston');

const logger = winston.createLogger({
    level: 'info',
    format: winston.format.combine(
        winston.format.timestamp(),
        winston.format.json()
    ),
    transports: [
        new winston.transports.File({ filename: 'logs/error.log', level: 'error' }),
        new winston.transports.File({ filename: 'logs/security.log', level: 'warn' }),
        new winston.transports.File({ filename: 'logs/combined.log' })
    ]
});

// 로그인 시도 기록
app.post('/api/auth/login', async (req, res) => {
    const { username, password } = req.body;
    const ip = req.ip || req.connection.remoteAddress;
    
    try {
        // ... 인증 로직
        
        if (!isPasswordValid) {
            logger.warn({
                event: 'LOGIN_FAILED',
                username,
                ip,
                timestamp: new Date().toISOString()
            });
            
            return res.status(401).json({ 
                success: false, 
                error: '아이디 또는 비밀번호가 올바르지 않습니다.' 
            });
        }
        
        logger.info({
            event: 'LOGIN_SUCCESS',
            username,
            ip,
            timestamp: new Date().toISOString()
        });
        
        // ... 토큰 발급
    } catch (error) {
        logger.error({
            event: 'LOGIN_ERROR',
            username,
            ip,
            error: error.message,
            timestamp: new Date().toISOString()
        });
        
        res.status(500).json({ success: false, error: '서버 오류가 발생했습니다.' });
    }
});

// API 접근 로그 미들웨어
app.use((req, res, next) => {
    const startTime = Date.now();
    
    res.on('finish', () => {
        const duration = Date.now() - startTime;
        logger.info({
            method: req.method,
            path: req.path,
            status: res.statusCode,
            duration,
            ip: req.ip,
            userAgent: req.get('user-agent'),
            user: req.user?.username || 'anonymous',
            timestamp: new Date().toISOString()
        });
    });
    
    next();
});
```

---

### 11. 에러 처리 미흡 - 정보 노출
**위치**: 여러 catch 블록

**문제점**:
- 상세한 에러 메시지가 클라이언트에 노출
- 스택 트레이스 노출 가능

**해결방안**:
```javascript
// ❌ 현재
catch (error) {
    console.error('근로자 조회 오류:', error);
    res.status(500).json({ success: false, error: error.message });  // ⚠️ 상세 에러 노출
}

// ✅ 권장
class AppError extends Error {
    constructor(message, statusCode = 500, isOperational = true) {
        super(message);
        this.statusCode = statusCode;
        this.isOperational = isOperational;
    }
}

// 에러 핸들링 미들웨어
app.use((err, req, res, next) => {
    err.statusCode = err.statusCode || 500;
    err.status = err.status || 'error';
    
    if (process.env.NODE_ENV === 'production') {
        // 운영 환경: 일반 메시지만
        if (err.isOperational) {
            res.status(err.statusCode).json({
                success: false,
                error: err.message
            });
        } else {
            // 프로그래밍 에러: 로그만 남기고 일반 메시지
            logger.error({
                error: err.message,
                stack: err.stack,
                timestamp: new Date().toISOString()
            });
            
            res.status(500).json({
                success: false,
                error: '서버 내부 오류가 발생했습니다.'
            });
        }
    } else {
        // 개발 환경: 상세 정보
        res.status(err.statusCode).json({
            success: false,
            error: err.message,
            stack: err.stack
        });
    }
});

// 사용 예시
app.get('/api/employees', requireAuth, async (req, res, next) => {
    try {
        const { data, error } = await supabase
            .from('employees')
            .select('*, companies(name)')
            .order('created_at', { ascending: false });
        
        if (error) {
            throw new AppError('근로자 목록을 불러올 수 없습니다.', 500);
        }
        
        res.json({ success: true, data: employees });
    } catch (error) {
        next(error);  // 에러 핸들러로 전달
    }
});
```

---

### 12. Race Condition (동시성 문제)
**위치**: `server.js:559-591`

**문제점**:
- TO(정원) 확인 시 동시 요청에 대한 처리 없음
- 코드 주석에서 인지하고 있음

**해결방안**:
```javascript
// Supabase RPC 함수 생성 (PostgreSQL)
-- Supabase Dashboard > SQL Editor에서 실행
CREATE OR REPLACE FUNCTION add_employee_with_to_check(
    p_company_id INT,
    p_name TEXT,
    p_hire_date DATE,
    p_hire_year INT,
    p_business_type TEXT
) RETURNS JSON AS $$
DECLARE
    v_to_count INT;
    v_current_count INT;
    v_new_employee JSON;
BEGIN
    -- 트랜잭션 시작 (암묵적)
    
    -- TO 확인 (FOR UPDATE로 행 잠금)
    SELECT to_count INTO v_to_count
    FROM company_to
    WHERE company_id = p_company_id AND year = p_hire_year
    FOR UPDATE;
    
    IF v_to_count IS NOT NULL THEN
        -- 현재 인원 확인
        SELECT COUNT(*) INTO v_current_count
        FROM employees
        WHERE company_id = p_company_id 
          AND hire_year = p_hire_year 
          AND resigned = false
        FOR UPDATE;
        
        -- TO 초과 확인
        IF v_current_count >= v_to_count THEN
            RAISE EXCEPTION 'TO 초과: %년 정원 %명 중 %명 재직', 
                p_hire_year, v_to_count, v_current_count;
        END IF;
    END IF;
    
    -- 근로자 추가
    INSERT INTO employees (company_id, name, hire_date, hire_year, business_type)
    VALUES (p_company_id, p_name, p_hire_date, p_hire_year, p_business_type)
    RETURNING row_to_json(employees.*) INTO v_new_employee;
    
    RETURN v_new_employee;
END;
$$ LANGUAGE plpgsql;

// Node.js에서 호출
app.post('/api/employees', requireAuth, async (req, res) => {
    try {
        const { companyId, name, hireDate, businessType } = req.body;
        const hireYear = hireDate ? new Date(hireDate).getFullYear() : new Date().getFullYear();
        
        const { data, error } = await supabase
            .rpc('add_employee_with_to_check', {
                p_company_id: companyId,
                p_name: name,
                p_hire_date: hireDate,
                p_hire_year: hireYear,
                p_business_type: businessType || '유형1'
            });
        
        if (error) {
            if (error.message.includes('TO 초과')) {
                return res.status(400).json({
                    success: false,
                    error: error.message
                });
            }
            throw error;
        }
        
        res.status(201).json({
            success: true,
            message: '근로자가 등록되었습니다.',
            data
        });
    } catch (error) {
        console.error('근로자 추가 오류:', error);
        res.status(500).json({ success: false, error: error.message });
    }
});
```

---

### 13. 비밀번호 정책 부족
**위치**: `server.js:235-240`

**문제점**:
- 비밀번호 최소 8자만 확인
- 복잡도 요구사항 없음 (대문자, 소문자, 숫자, 특수문자)

**해결방안**:
```javascript
// 비밀번호 검증 함수
function validatePassword(password) {
    const minLength = 10;
    const hasUpperCase = /[A-Z]/.test(password);
    const hasLowerCase = /[a-z]/.test(password);
    const hasNumber = /\d/.test(password);
    const hasSpecialChar = /[!@#$%^&*(),.?":{}|<>]/.test(password);
    
    const errors = [];
    
    if (password.length < minLength) {
        errors.push(`비밀번호는 최소 ${minLength}자 이상이어야 합니다.`);
    }
    if (!hasUpperCase) {
        errors.push('대문자를 최소 1개 포함해야 합니다.');
    }
    if (!hasLowerCase) {
        errors.push('소문자를 최소 1개 포함해야 합니다.');
    }
    if (!hasNumber) {
        errors.push('숫자를 최소 1개 포함해야 합니다.');
    }
    if (!hasSpecialChar) {
        errors.push('특수문자를 최소 1개 포함해야 합니다.');
    }
    
    return {
        isValid: errors.length === 0,
        errors
    };
}

// 비밀번호 변경 시 적용
app.post('/api/auth/change-password', requireAuth, async (req, res) => {
    try {
        const { currentPassword, newPassword } = req.body;
        
        // 비밀번호 검증
        const validation = validatePassword(newPassword);
        if (!validation.isValid) {
            return res.status(400).json({
                success: false,
                error: '비밀번호 요구사항을 충족하지 않습니다.',
                details: validation.errors
            });
        }
        
        // 현재 비밀번호와 동일한지 확인
        const isSameAsOld = await bcrypt.compare(newPassword, ADMIN_PASSWORD_HASH);
        if (isSameAsOld) {
            return res.status(400).json({
                success: false,
                error: '현재 비밀번호와 동일한 비밀번호는 사용할 수 없습니다.'
            });
        }
        
        // ... 비밀번호 변경 로직
    } catch (error) {
        console.error('비밀번호 변경 오류:', error);
        res.status(500).json({ success: false, error: '서버 오류가 발생했습니다.' });
    }
});
```

---

## 🟢 Low (낮음) - 중장기 개선

### 14. HTTPS 강제 리다이렉트 없음
**해결방안**:
```javascript
// HTTP to HTTPS 리다이렉트
app.use((req, res, next) => {
    if (process.env.NODE_ENV === 'production' && !req.secure) {
        return res.redirect(`https://${req.headers.host}${req.url}`);
    }
    next();
});
```

---

### 15. 데이터 백업 및 복구 계획 미비
**권장사항**:
- Supabase 자동 백업 활성화 확인
- 수동 백업 스크립트 작성
- 복구 절차 문서화

```bash
# 백업 스크립트 예시
#!/bin/bash
BACKUP_DIR="./backups"
DATE=$(date +%Y%m%d_%H%M%S)

# Supabase 백업 (supabase-cli 사용)
npx supabase db dump -f "$BACKUP_DIR/backup_$DATE.sql"

# S3 또는 클라우드 스토리지에 업로드
# aws s3 cp "$BACKUP_DIR/backup_$DATE.sql" s3://my-backup-bucket/
```

---

## 📊 종합 우선순위 로드맵

### 🚨 즉시 조치 (1주 이내)
1. ✅ 민감 정보를 환경변수로 이동 + Git 히스토리 정리
2. ✅ CORS 정책 엄격하게 설정
3. ✅ 비밀번호 해시 응답 제거

### ⚡ 단기 조치 (1개월 이내)
4. ✅ 레이트 리미팅 적용
5. ✅ 보안 헤더 적용 (Helmet)
6. ✅ 쿠키 보안 강화 (sameSite: strict)
7. ✅ XSS 방어 (입력 검증 + DOMPurify)

### 🔧 중기 개선 (3개월 이내)
8. ✅ Access Token + Refresh Token 구조
9. ✅ 로깅 및 감사 시스템
10. ✅ Race Condition 해결 (DB 트랜잭션)
11. ✅ 비밀번호 정책 강화

### 📈 장기 개선 (6개월 이내)
12. ✅ WAF (Web Application Firewall) 도입
13. ✅ 침투 테스트 (Penetration Testing)
14. ✅ 보안 교육 및 인식 제고
15. ✅ OWASP Top 10 전체 점검

---

## 🛡️ 고도화 권장사항

### 1. **다중 인증 (MFA) 도입**
- 관리자 계정에 OTP (TOTP) 추가
- Google Authenticator, Authy 등 지원

```javascript
// npm install speakeasy qrcode
const speakeasy = require('speakeasy');
const QRCode = require('qrcode');

// MFA 설정
app.post('/api/auth/mfa/setup', requireAuth, async (req, res) => {
    const secret = speakeasy.generateSecret({
        name: `지원금관리 (${req.user.username})`,
        issuer: '일자리도약장려금'
    });
    
    // QR 코드 생성
    const qrCodeUrl = await QRCode.toDataURL(secret.otpauth_url);
    
    // secret을 암호화하여 DB 저장
    // ... 저장 로직
    
    res.json({
        success: true,
        secret: secret.base32,
        qrCode: qrCodeUrl
    });
});

// MFA 검증
app.post('/api/auth/mfa/verify', requireAuth, async (req, res) => {
    const { token } = req.body;
    
    // DB에서 사용자의 secret 가져오기
    const userSecret = '...';
    
    const verified = speakeasy.totp.verify({
        secret: userSecret,
        encoding: 'base32',
        token,
        window: 2  // ±2 time steps (60초)
    });
    
    if (verified) {
        res.json({ success: true, message: 'MFA 인증 성공' });
    } else {
        res.status(401).json({ success: false, error: 'MFA 코드가 올바르지 않습니다.' });
    }
});
```

---

### 2. **데이터 암호화**
- 민감 정보 (주민등록번호, 비밀번호 등) 암호화 저장
- AES-256-GCM 사용 권장

```javascript
// npm install crypto-js
const crypto = require('crypto');

const ENCRYPTION_KEY = process.env.ENCRYPTION_KEY; // 32 bytes
const IV_LENGTH = 16;

function encrypt(text) {
    const iv = crypto.randomBytes(IV_LENGTH);
    const cipher = crypto.createCipheriv('aes-256-cbc', Buffer.from(ENCRYPTION_KEY, 'hex'), iv);
    let encrypted = cipher.update(text);
    encrypted = Buffer.concat([encrypted, cipher.final()]);
    return iv.toString('hex') + ':' + encrypted.toString('hex');
}

function decrypt(text) {
    const parts = text.split(':');
    const iv = Buffer.from(parts.shift(), 'hex');
    const encryptedText = Buffer.from(parts.join(':'), 'hex');
    const decipher = crypto.createDecipheriv('aes-256-cbc', Buffer.from(ENCRYPTION_KEY, 'hex'), iv);
    let decrypted = decipher.update(encryptedText);
    decrypted = Buffer.concat([decrypted, decipher.final()]);
    return decrypted.toString();
}

// 사용 예시
app.post('/api/companies', requireAuth, async (req, res) => {
    const { ceoIdNumber } = req.body;
    
    // 주민등록번호 암호화
    const encryptedIdNumber = encrypt(ceoIdNumber);
    
    const { data, error } = await supabase
        .from('companies')
        .insert([{
            ...req.body,
            ceo_id_number: encryptedIdNumber
        }])
        .select()
        .single();
    
    // 응답 시 복호화
    data.ceo_id_number = decrypt(data.ceo_id_number);
    
    res.json({ success: true, data });
});
```

---

### 3. **API 버전 관리**
- API 경로에 버전 추가 (`/api/v1/...`)
- 하위 호환성 유지

```javascript
// v1 라우터
const v1Router = express.Router();

v1Router.get('/companies', requireAuth, async (req, res) => {
    // ... v1 로직
});

app.use('/api/v1', v1Router);

// 향후 v2 추가 시
const v2Router = express.Router();
v2Router.get('/companies', requireAuth, async (req, res) => {
    // ... v2 로직 (개선된 버전)
});

app.use('/api/v2', v2Router);
```

---

### 4. **모니터링 및 알림**
- Sentry, Datadog 등 APM 도구 도입
- 보안 이벤트 실시간 알림

```javascript
// npm install @sentry/node
const Sentry = require('@sentry/node');

Sentry.init({
    dsn: process.env.SENTRY_DSN,
    environment: process.env.NODE_ENV,
    tracesSampleRate: 1.0
});

// 에러 추적
app.use(Sentry.Handlers.requestHandler());
app.use(Sentry.Handlers.errorHandler());

// 보안 이벤트 알림
function sendSecurityAlert(event) {
    Sentry.captureMessage(`보안 이벤트: ${event.type}`, {
        level: 'warning',
        tags: {
            security: true,
            event_type: event.type
        },
        extra: event
    });
    
    // 추가로 Slack, Discord 등으로 알림
    // await notifySlack(`🚨 보안 이벤트: ${event.type}`);
}

// 사용 예시
app.post('/api/auth/login', async (req, res) => {
    // ... 로그인 실패 시
    if (!isPasswordValid) {
        sendSecurityAlert({
            type: 'LOGIN_FAILED',
            username,
            ip: req.ip,
            userAgent: req.get('user-agent')
        });
        // ...
    }
});
```

---

### 5. **정기 보안 점검 체크리스트**

#### 월간
- [ ] 로그인 실패 로그 분석
- [ ] API 사용량 이상 패턴 확인
- [ ] 의존성 취약점 스캔 (`npm audit`)
- [ ] SSL 인증서 만료일 확인

#### 분기
- [ ] 패스워드 변경 권장
- [ ] 접근 권한 재검토
- [ ] 데이터 백업 복구 테스트
- [ ] 보안 정책 업데이트

#### 연간
- [ ] 침투 테스트 (Penetration Testing)
- [ ] 전체 코드 보안 감사 (Security Audit)
- [ ] 재해 복구 훈련 (Disaster Recovery)
- [ ] 보안 교육 실시

---

## 📚 참고 자료

- **OWASP Top 10**: https://owasp.org/www-project-top-ten/
- **Node.js Security Best Practices**: https://nodejs.org/en/docs/guides/security/
- **Express.js Security**: https://expressjs.com/en/advanced/best-practice-security.html
- **Supabase Security**: https://supabase.com/docs/guides/platform/security
- **NIST Cybersecurity Framework**: https://www.nist.gov/cyberframework

---

## ✅ 결론

이 시스템은 **개인정보 보호법**과 **정보통신망법**의 적용을 받는 민감한 정보를 다루고 있어 보안이 매우 중요합니다. 특히 **Critical 레벨 3건**은 즉시 조치가 필요하며, 이를 방치할 경우 **개인정보 유출 및 법적 책임**이 발생할 수 있습니다.

위에서 제시한 권장사항을 단계별로 적용하면, 보안 수준을 크게 향상시킬 수 있습니다. 추가 지원이 필요하시면 언제든지 문의하세요.

---

**작성자**: AI Security Analyzer  
**검토일**: 2026-01-29  
**버전**: 1.0

